input15 <- read.table("C:/Users/Zoe/Documents/R Scripts/Adventofcode2021/day15test.txt",              # TXT data file indicated as string or full path to the file
                    header = FALSE,    # Whether to display the header (TRUE) or not (FALSE)
                    sep = ",")          # Separator of the columns of the file
x<-1
y<-1
input15[x,y]
